function pentagonFunction(){
    const p = defalultFuntionById('pentagon-p');
    const b = defalultFuntionById('pentagon-b');

    const area = 0.5 * p * b;

    defaultResutbyId('pentagon-area-span', area);
    defaultResutbyId('area-result', area);
}